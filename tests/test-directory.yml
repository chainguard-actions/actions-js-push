name: Test push from subdirectory
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Configure git and set up fake git to intercept push
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"

          # Create a local bare repo to receive the push
          git init --bare /tmp/test-remote-directory.git
          git push /tmp/test-remote-directory.git HEAD:refs/heads/act-harness

          # Find the real git binary (before we shadow it)
          REAL_GIT="$(which git)"

          # Create a fake git wrapper that redirects github.com pushes to local repo
          mkdir -p /tmp/fake-git-directory
          {
            echo '#!/bin/sh'
            echo "REAL_GIT=\"$REAL_GIT\""
            echo 'if [ "$1" = "push" ]; then'
            echo '  shift'
            echo '  newargs=""'
            echo '  for arg in "$@"; do'
            echo '    case "$arg" in'
            echo '      https://*github.com*) arg="file:///tmp/test-remote-directory.git" ;;'
            echo '    esac'
            echo '    newargs="${newargs:+$newargs }$arg"'
            echo '  done'
            echo '  exec "$REAL_GIT" push $newargs'
            echo 'fi'
            echo 'exec "$REAL_GIT" "$@"'
          } > /tmp/fake-git-directory/git
          chmod +x /tmp/fake-git-directory/git
          # Verify the fake git works
          /tmp/fake-git-directory/git --version

      - name: Create a test file in the workspace
        run: |
          echo "directory-test-$(date +%s)" > tests/test-directory-temp.txt
          echo "Created test file"

      - name: Run action with directory input pointing to tests subdirectory
        id: action
        uses: .
        env:
          GITHUB_ACTOR: x-access-token
          PATH: /tmp/fake-git-directory:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
        with:
          github_token: ${{ github.token }}
          branch: act-harness
          author_email: test@example.com
          author_name: Test User
          directory: tests
          force: 'true'

      - name: Assert action completed successfully from subdirectory
        run: |
          echo "PASS: action completed operating from subdirectory"

      - name: Clean up temp file
        if: always()
        run: rm -f tests/test-directory-temp.txt
